# core modules
Shared Android building blocks.
- `common`: cross-cutting utilities and constants
- `network`: networking contracts and serialization setup
- `database`: persistence abstractions
- `preferences`: local settings and key-value storage
- `ui`: Compose theming and reusable components
- `navigation`: navigation routes and helpers
