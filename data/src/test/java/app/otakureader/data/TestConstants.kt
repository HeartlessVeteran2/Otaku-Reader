package app.otakureader.data

/**
 * Test constants for data layer tests.
 *
 * These are clearly fake API keys used for testing purposes only.
 * They follow a fake format to avoid false positives from security scanners.
 */
object TestConstants {
    /**
     * Fake API key for testing. NOT a real Gemini API key.
     * Format: TEST_GEMINI_API_KEY_[description]
     */
    const val FAKE_API_KEY = "TEST_GEMINI_API_KEY_SomeFakeKey"
}
