<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
<!-- Required for extension installation: allows the app to install extension APKs
         that the user explicitly downloads from trusted extension repositories.
         This permission is only exercised via ExtensionInstaller after the user
         confirms the installation through the Extensions screen. No background or
         automatic APK installs are performed. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Camera: used for QR code scanning (library share/import via QR) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Required for the onboarding "disable battery optimization" step: without this
         permission, ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS silently does nothing.
         Used so library-update and download workers aren't killed by Doze. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Storage permissions for legacy chapter downloads, local manga, covers, and CBZ export -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Local source reads CBZ/ZIP/EPUB from a user-chosen folder via java.io.File. On
         Android 11+ scoped storage blocks arbitrary File access to shared storage, so the
         local source needs All-Files-Access. Requested at runtime from the Local Source
         screen; this is a sideloaded app (not Play-distributed), so the policy gate does
         not apply. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Package visibility: required to discover and manage Tachiyomi-compatible extension
         packages. Uses targeted <queries> below instead of QUERY_ALL_PACKAGES so this
         does not trigger a Play Store policy review. -->
    <queries>
        <!-- Known Tachiyomi/Mihon extension package prefixes -->
        <intent>
            <action android:name="tachiyomi.extension" />
        </intent>
    </queries>


    <application
        android:name=".OtakuReaderApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OtakuReaderBase">

        <!-- Sentry crash reporting (#952) is opt-in and initialized manually at runtime via
             CrashReporter only after the user supplies a DSN and opts in. Disable the SDK's
             automatic ContentProvider init so nothing in Sentry runs at process start before
             the user has consented (and so it cannot fail during early startup). -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OtakuReaderSplash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep links: MangaDex -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mangadex.org" android:pathPrefix="/title" />
                <data android:scheme="https" android:host="www.mangadex.org" android:pathPrefix="/title" />
            </intent-filter>

            <!-- Deep links: MangaPlus (Shonen Jump) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mangaplus.shueisha.co.jp" android:pathPrefix="/titles" />
            </intent-filter>

            <!-- Deep links: MangaSee -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mangasee123.com" android:pathPrefix="/manga" />
            </intent-filter>

            <!-- Deep links: MangaFire -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mangafire.to" android:pathPrefix="/manga" />
            </intent-filter>

            <!-- Deep links: Bato.to -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="bato.to" android:pathPrefix="/title" />
                <data android:scheme="https" android:host="bato.to" android:pathPrefix="/series" />
            </intent-filter>

            <!-- Deep links: Webtoons -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.webtoons.com" android:pathPattern="/.*/list.*" />
            </intent-filter>

            <!-- OAuth callback deep links: Kitsu -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app.otakureader" android:host="kitsu-oauth" />
            </intent-filter>

            <!-- OAuth callback deep links: MyAnimeList -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app.otakureader" android:host="mal-oauth" />
            </intent-filter>

            <!-- OAuth callback deep links: Shikimori -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app.otakureader" android:host="shikimori-oauth" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app.otakureader" android:host="anilist-oauth" />
            </intent-filter>
            
            <!-- Share intent filter for text/plain -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <receiver
            android:name="app.otakureader.core.extension.receiver.ExtensionInstallReceiver"
            android:exported="false">
            <!-- System package-manager events (shared/installed extensions) -->
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
            <!-- App-internal events for private/sideloaded extensions -->
            <intent-filter>
                <action android:name="app.otakureader.ACTION_EXTENSION_ADDED" />
                <action android:name="app.otakureader.ACTION_EXTENSION_REPLACED" />
                <action android:name="app.otakureader.ACTION_EXTENSION_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Continue Reading Widget -->
        <receiver
            android:name=".widget.ContinueReadingWidgetReceiver"
            android:exported="true"
            android:label="@string/continue_reading_widget_label"
            android:permission="android.permission.BIND_APPWIDGET">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/continue_reading_widget_info" />
        </receiver>

        <!-- Now Reading Widget -->
        <receiver
            android:name=".widget.NowReadingWidgetReceiver"
            android:exported="true"
            android:label="@string/now_reading_widget_label"
            android:permission="android.permission.BIND_APPWIDGET">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/now_reading_widget_info" />
        </receiver>

        <!-- Recent Updates Widget -->
        <receiver
            android:name=".widget.RecentUpdatesWidgetReceiver"
            android:exported="true"
            android:label="@string/recent_updates_widget_label"
            android:permission="android.permission.BIND_APPWIDGET">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recent_updates_widget_info" />
        </receiver>

        <!-- Large Home Widget — cover-art carousel (#949) -->
        <receiver
            android:name=".widget.HomeWidgetReceiver"
            android:exported="true"
            android:label="@string/home_widget_label"
            android:permission="android.permission.BIND_APPWIDGET">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/home_widget_info" />
        </receiver>

        <!--
            WorkManager foreground service: declare dataSync type so that library-update
            and backup workers can run as foreground services on Android 14+ without
            a ForegroundServiceStartNotAllowedException.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
